import Background from '../../lowComponents/Background';
import LoginForm from '../../lowComponents/LoginForm';

const Login = () => {
  const name = 'asdf';
  return (
    <Background>
      <LoginForm />
    </Background>
  );
};

export default Login;
